---
name: open
description: Open the Control Center dashboard in the default browser
disable-model-invocation: true
---

Open the Control Center dashboard.

Run this command:
```bash
open http://localhost:48900
```

If the server is not running, it will auto-start on the next Claude Code session.
